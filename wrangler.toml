name = "meaicon"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Static assets configuration — serves the Eleventy build output
[assets]
directory = "./_site"
binding = "ASSETS"

[build]
command = "npm install && npm run build"
